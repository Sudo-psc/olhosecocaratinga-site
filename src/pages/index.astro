---
export const prerender = false;
import Layout from '../layouts/Layout.astro';
import { getPosts } from '../lib/wordpress'; // Use the lib helper

// Fetch latest 3 posts for the blog section
const posts = await getPosts({ per_page: 3 });
---

<Layout title="Olho Seco Caratinga | Tratamento Especializado">
  <!-- Hero Section -->
  <section class="relative bg-gradient-to-br from-blue-50 to-white pt-20 pb-32 overflow-hidden">
    <div class="absolute top-0 right-0 w-1/3 h-full bg-blue-100/30 blur-3xl rounded-full -translate-y-1/2 translate-x-1/2"></div>
    <div class="container mx-auto px-6 relative z-10">
      <div class="max-w-3xl">
        <span class="inline-block py-1 px-3 rounded-full bg-blue-100 text-blue-700 text-sm font-semibold mb-6">
          Especialistas em Oftalmologia
        </span>
        <h1 class="text-5xl md:text-6xl font-bold text-gray-900 leading-tight mb-6">
          Recupere o Conforto da Sua <span class="text-blue-600">Visão</span>
        </h1>
        <p class="text-xl text-gray-600 mb-8 leading-relaxed max-w-2xl">
          Sofre com irritação, vermelhidão ou sensação de areia nos olhos? 
          Oferecemos diagnósticos precisos e tratamentos avançados para a Síndrome do Olho Seco em Caratinga.
        </p>
        <div class="flex flex-col sm:flex-row gap-4">
          <a href="/agendar" class="px-8 py-4 bg-blue-600 text-white rounded-xl font-semibold shadow-lg shadow-blue-200 hover:bg-blue-700 hover:-translate-y-1 transition-all text-center">
            Agendar Avaliação
          </a>
          <a href="#sintomas" class="px-8 py-4 bg-white text-blue-600 border border-blue-100 rounded-xl font-semibold hover:bg-blue-50 transition-all text-center">
            Saiba Mais
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Symptoms Section -->
  <section id="sintomas" class="py-24 bg-white">
    <div class="container mx-auto px-6">
      <div class="text-center max-w-2xl mx-auto mb-16">
        <h2 class="text-3xl font-bold text-gray-900 mb-4">Sinais de Alerta</h2>
        <p class="text-gray-600">
          A Síndrome do Olho Seco pode se manifestar de diversas formas. Identificar os sintomas é o primeiro passo para o tratamento.
        </p>
      </div>

      <div class="grid md:grid-cols-3 gap-8">
        {[
          { title: "Sensação de Areia", desc: "Desconforto persistente como se houvesse um corpo estranho no olho." },
          { title: "Vermelhidão", desc: "Irritação frequente, especialmente ao acordar ou após uso de telas." },
          { title: "Visão Embaçada", desc: "Flutuação na qualidade visual que melhora ao piscar." },
        ].map((item) => (
          <div class="p-8 rounded-2xl bg-gray-50 hover:bg-blue-50 transition-colors border border-gray-100 hover:border-blue-100 group">
            <h3 class="text-xl font-bold text-gray-900 mb-3 group-hover:text-blue-600 transition-colors">{item.title}</h3>
            <p class="text-gray-600">{item.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Blog Preview Section (WordPress Content) -->
  <section class="py-24 bg-gray-50">
    <div class="container mx-auto px-6 max-w-6xl">
      <div class="flex justify-between items-end mb-12">
        <div>
          <h2 class="text-3xl font-bold text-gray-900 mb-2">Artigos Recentes</h2>
          <p class="text-gray-600">Informações e novidades sobre saúde ocular.</p>
        </div>
        <a href="/blog" class="hidden md:inline-flex items-center text-blue-600 font-medium hover:text-blue-700">
          Ver todos os artigos
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
          </svg>
        </a>
      </div>

      <div class="grid md:grid-cols-3 gap-8">
        {posts.length > 0 ? posts.map((post) => (
          <article class="bg-white rounded-2xl overflow-hidden shadow-sm hover:shadow-xl transition-all duration-300 group">
            {post._embedded?.['wp:featuredmedia']?.[0]?.source_url && (
              <div class="h-48 overflow-hidden">
                <img 
                  src={post._embedded['wp:featuredmedia'][0].source_url} 
                  alt={post._embedded['wp:featuredmedia'][0].alt_text || post.title.rendered}
                  class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
                />
              </div>
            )}
            <div class="p-6">
              <h3 class="text-xl font-bold text-gray-900 mb-3 line-clamp-2 group-hover:text-blue-600 transition-colors">
                <a href={`/blog/${post.slug}`} set:html={post.title.rendered} />
              </h3>
              <div class="text-gray-600 text-sm mb-4 line-clamp-3" set:html={post.excerpt.rendered} />
              <a href={`/blog/${post.slug}`} class="inline-block text-blue-600 font-medium hover:underline">
                Ler mais
              </a>
            </div>
          </article>
        )) : (
          <div class="col-span-3 text-center py-12">
            <p class="text-gray-500">Nenhum artigo encontrado no momento.</p>
          </div>
        )}
      </div>
      
      <div class="mt-8 text-center md:hidden">
        <a href="/blog" class="btn btn-secondary">Ver todos os artigos</a>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-24 bg-blue-600 relative overflow-hidden">
    <div class="absolute inset-0 bg-[url('/pattern.svg')] opacity-10"></div>
    <div class="container mx-auto px-6 relative z-10 text-center">
      <h2 class="text-3xl md:text-4xl font-bold text-white mb-6">Pronto para cuidar da sua visão?</h2>
      <p class="text-blue-100 text-lg mb-8 max-w-2xl mx-auto">
        Agende sua consulta hoje mesmo e descubra como podemos melhorar sua qualidade de vida através de tratamentos personalizados.
      </p>
      <a href="/agendar" class="inline-block px-8 py-4 bg-white text-blue-600 rounded-xl font-bold hover:bg-blue-50 transition-colors shadow-lg">
        Agendar Consulta Agora
      </a>
    </div>
  </section>
</Layout>
